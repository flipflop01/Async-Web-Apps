Youtube Credentials and Questions

API Key: AIzaSyCLca5LG3szn-Q24AmBOWyytenluU6DbQU
-----------------------
Explain(from GitHub API w/ JSON sample):

function displayGitHubSearchData(data) {
  const results = data.items.map((item, index) => renderResult(item));
  $('.js-search-results').html(results);
}
------------------------------------------------------------
$.ajax(settings)
----------------
callback
--------